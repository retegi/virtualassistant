from django.apps import AppConfig

class TuAsistenteInteligenteConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'applications.tuasistenteinteligente'  # Debe coincidir con la estructura de carpetas
